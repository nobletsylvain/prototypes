{
  "name": "prototypes-ios",
  "private": true,
  "type": "module",
  "description": "Coque Capacitor iOS qui embarque le hub + tous les core loops (TestFlight).",
  "scripts": {
    "build": "node build-www.mjs",
    "sync": "npm run build && cap sync ios",
    "ios": "npm run build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
